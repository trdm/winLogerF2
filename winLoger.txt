(c) trdmval: trdmval@gmail.com
winLoger.exe 
1. сканирует окна с периодичностью 10 секунд и если встречает 
окна с фрагментами pзголовков, описанные в файле winLoger.set.txt, логирует их заголовки.	
¬ следующее сканированние запоминаютс€ только измененные заголовки относительно фрагментов.
заголовки запоминаютс€ вместе с хендлами и классами окна. 
можно настроить сопоставление как по части заголовка, так и по названию класса окна.
